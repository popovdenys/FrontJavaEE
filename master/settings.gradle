/**
 * Customizing JavaEE Application
 */

rootProject.name = 'po'

includeFlat 'model', 'repository', 'web-server', 'web-client', 'app-client'

rootProject.children.each {
	it.buildFileName = it.name + '.gradle'
}
